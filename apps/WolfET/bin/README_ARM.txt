1) install WolfET on a Windows7/8 machine.
2) copy the contents of the etmain folder into this etmain folder (config files, pak files, etc.)
3) copy version.inf to this root folder


note: you cannot use this to host a pure sever, you must set "sv_pure" to 0